[package]
name = "audio_capture"
version = "0.1.0"
edition = "2021"

[features]
mock_audio = []
standalone = []

[[example]]
name = "standalone_capture"
required-features = ["standalone"]

[dependencies]
crossbeam = "0.8"
libc = "0.2"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = "0.3"
