[package]
name = "trigger"
version = "0.1.0"
edition = "2021"

[features]
mock_scores = []
standalone = []

[dependencies]
crossbeam-channel = "0.5"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = "0.3"
