//! Gestion du NSStatusItem dans la barre de menu macOS.
//!
//! Affiche l'icône/emoji et gère le menu contextuel.

/// Représente l'icône dans la barre de menu macOS.
pub struct MenuBarIcon {
    // Sera enrichi lors de P2
}

impl MenuBarIcon {
    /// Crée une nouvelle icône dans la barre de menu.
    pub fn new() -> Self {
        Self {}
    }
}
